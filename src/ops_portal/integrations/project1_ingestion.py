"""Read-only adapter onto Project 1's ingestion output.

TODO: read PROJECT1_INGESTION_SOURCE for ingestion health — records
processed, last run, failures — for the dashboard screen. Read-only: this
adapter never writes back into Project 1's own database.
"""
