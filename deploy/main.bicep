// TODO: Container App, Postgres Flexible Server, Key Vault, managed
// identity, HTTPS with a real certificate. Secrets resolved from Key Vault
// via managed identity — never a plaintext secret in this template.
