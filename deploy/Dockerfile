# TODO: multi-stage build — install package + [postgres] extra, copy src/,
# run as non-root, CMD uvicorn ops_portal.app:app --host 0.0.0.0 --port 8000.
# .env is never copied into the image (see .dockerignore).
